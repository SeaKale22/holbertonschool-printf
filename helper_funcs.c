#include "main.h"

/**
 * char_comp - compares two characters
 * @c1: char 1
 * @c2: char 2
 * Return: 0 if equal and 1 if not
 */

int char_comp(char c1, char c2)
{
	if (c1 == c2)
		return (0);
	else
		return (1);
}
